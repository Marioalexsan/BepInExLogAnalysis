{
  "name": "bundled_rules/version_detect",
  "description": "Rule lists for detecting game versions through log file output.",
  "rules": [
    {
      "sourceFilter": ["Homebrewery"],
      "gameFilter": ["ATLYSS"],
      "contentPattern": "Waking up at (?:\\S*) (?:\\S*) (?:AM|PM) UTC\\.\\.\\. Game Version is: (?<game_version>[\\S\\ ]*)",
      "scoring": {
        "ignore": true
      },
      "transform": {
        "section": "Metadata",
        "rule": "set_fields"
      }
    }
  ]
}